opipeline {
    agent {
    label{
        label "built-in"
        customWorkspace "/mnt"
    }
    }
    
    stages  {
        stage("getting git repository"){
            steps {
                echo "clone project"
                
            }
        }
        stage ("creating container"){
            steps {
                
                
            }
        }
    }
}
